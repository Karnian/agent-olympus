{
  "name": "agent-olympus",
  "version": "0.7.2",
  "description": "Self-driving AI orchestrator plugin for Claude Code",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "test": "node --test scripts/test/**/*.test.mjs",
    "check": "for f in scripts/*.mjs scripts/lib/*.mjs; do node --check \"$f\" && echo \"OK: $f\"; done"
  }
}
